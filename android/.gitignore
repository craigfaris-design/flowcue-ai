# Everything here is generated by `bubblewrap build` from twa-manifest.json --
# a full Gradle/Android project plus build caches and signed binaries, none
# of which belongs in source control (large, regeneratable, and the .aab/.apk
# are build artifacts you'd re-produce per release anyway). Only
# twa-manifest.json and README.md are the real source of truth here.
app/
gradle/
local.properties
.gradle/
build/
gradlew
gradlew.bat
build.gradle
settings.gradle
gradle.properties
store_icon.png
manifest-checksum.txt
*.aab
*.apk
*.apk.idsig
